#include <stdio.h>
#include "cs1010.c"
#include <stdlib.h>

/*
double average(long k, long arr[k])
{
    double avg = 0.0;
    for (long i = 0; i < k; i++)
    {
        avg += arr[i];    
    }
    return avg/k;
}

int main(int argc, char *argv[])
{
    long num_elem;
    num_elem = cs1010_read_long();
    long *marks;
    marks = cs1010_read_long_array(num_elem);
    printf("%f\n", average(num_elem, marks));
    return 0;
}
*/
int main()
{
  long a = 0;
  printf("%ld\n", max(1, 10));
}
